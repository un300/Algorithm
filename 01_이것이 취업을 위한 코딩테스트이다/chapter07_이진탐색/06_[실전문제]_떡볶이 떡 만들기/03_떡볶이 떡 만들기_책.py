# N, M = map(int, input().split())
# dduk_list = list(map(int, input().split()))

# start = 0
# end   = max(dduk_list)

# H = (start + end) // 2


# cutting_list = list(map(lambda x : x-H if x-H >= 0 else 0, dduk_list))
# s = sum(cutting_list)
# while M > s:
